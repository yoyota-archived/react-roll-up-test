name: Greet Everyone
on: [push]

jobs:
  build:
    name: Greeting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: test
        uses: docker://alpine
        with:
          entrypoint: |
            "/bin/sh -c"
          args: |
            "apk add curl wget && ls"
